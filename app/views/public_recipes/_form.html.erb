<%= form_with(model: public_recipe) do |form| %>
  <% if public_recipe.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(public_recipe.errors.count, "error") %> prohibited this public_recipe from being saved:</h2>

      <ul>
        <% public_recipe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
